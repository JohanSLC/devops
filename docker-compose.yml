version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      - db

  db:
    image: mysql:8
    environment:
      MYSQL_DATABASE: SistemaInventarioColegio
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"